"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _init_panels = _interopRequireDefault(require("./init_panels"));

describe('manager.ui.config.init_panels', function () {
  test('should call the selectAddonPanel with first panel name', function () {
    var actions = {
      ui: {
        selectAddonPanel: jest.fn()
      }
    };
    var provider = {
      getPanels: function getPanels() {
        return {
          test1: {},
          test2: {},
          test3: {}
        };
      }
    };
    (0, _init_panels.default)({
      provider: provider
    }, actions);
    expect(actions.ui.selectAddonPanel).toHaveBeenCalledWith('test1');
  });
});